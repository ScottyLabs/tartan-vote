# Backend API base (e.g. http://localhost:3000). Leave empty for same-origin.
PUBLIC_API_BASE=

# better-auth endpoint
PUBLIC_BETTER_AUTH_BASE_URL=http://localhost:3005/api/auth

# OAuth provider id configured on the auth server
PUBLIC_BETTER_AUTH_PROVIDER_ID=cmu-sso
